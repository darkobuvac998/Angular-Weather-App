import { Routes } from "@angular/router";
import { WeatherIconComponent } from "./navigation/weather-icon/weather-icon.component";

export const appRoutes: Routes = [
    {path: 'weather', component: WeatherIconComponent}
]